# Backtesting System — Progress Tracker

## Active Skill: `/new-feature`
## Feature: Comprehensive Backtesting System
## Started: 2026-06-05

---

## Status Overview

| Phase | Status | Notes |
|-------|--------|-------|
| Step 1: Discovery | COMPLETED | 5 discovery agents, full summary written |
| Step 2: Requirements Brainstorm | COMPLETED | 5 rounds, 22 agents, 305 requirements |
| Step 3: Architecture Document | COMPLETED | Created + reviewed (1 round), all Critical/High fixed |
| Step 4: Specification | COMPLETED | spec.md created |
| Step 5: Spec Review | COMPLETED | 7 rounds × 5 agents. All C/H fixed. R7: unanimously CLEAN |
| Step 6: Implementation Plan | COMPLETED | 7 phases, ~40 tasks, all reviews passed |
| Step 7: Plan Review | COMPLETED | 3 rounds + traceability audit + 5-agent codebase consistency audit. ALL critical/high fixed. |
| Step 8: Planning Summary | COMPLETED | |
| Step 9: Create Worktree | COMPLETED | worktree-backtesting-system |
| Step 10: Validate Plan | COMPLETED | Migration 37, all files exist, trading_rules.py done |
| Step 11: Implementation Tracker | COMPLETED | |
| Step 12: Per-Phase Implementation | IN_PROGRESS | P1-P5 ALL GATES PASSED. P4 committed b7cd140. P5 committed 7cbd9e5. **P6 Frontend ALL 4 GATES PASSED** — 12a/12b impl+validate (Tasks 6.1-6.8 + 6.5b, 210 tests), 12c-12f review cycle ran 12 rounds across correctness/a11y/contract/security/testing/plan-compliance/adversarial lenses, **2 consecutive clean rounds (R11+R12) → CONVERGED ship-ready**. tsc+build clean. NEXT: P6 12g commit, then P7 Integration. |
| Step 13: Cross-Phase Validation | COMPLETED | 6 rounds; found+fixed 8 cross-phase bugs (drawdown flat-zero, NUMERIC overflow x2+migration40, cancel race, equity/summary json-safe, empty-signals UX, downsample trough). 2 clean rounds. |
| Step 14: Final Review | IN_PROGRESS | 20-25 rounds (2 clean exit) |
| Step 15: Final Validation | PENDING | |
| Step 16: Traceability | PENDING | |
| Step 17: Readiness Check | PENDING | |
| Step 18: Merge/PR | PENDING | |
| Step 19: Summary | PENDING | |

---

## Activity Log

| Timestamp | Step | Activity | Result |
|-----------|------|----------|--------|
| 2026-06-05 | 1 | Started /new-feature skill | Created tracker |
| 2026-06-05 | 1 | Codebase discovery | COMPLETED — discovery-summary.md written |
| 2026-06-05 | 2 | Requirements brainstorm | COMPLETED — 5 rounds, 305 reqs, near-clean R5 |
| 2026-06-05 | 3 | Architecture document | COMPLETED — reviewed, all critical fixes applied |
| 2026-06-05 | 4 | Specification | IN_PROGRESS |
| 2026-06-06 | 12 | P6 Frontend 12a impl (Tasks 6.1-6.8) | COMPLETED — 16 backtest source files, 12 test files, 102 tests |
| 2026-06-06 | 12 | P6 Frontend 12b validate | COMPLETED — 102 tests pass, tsc clean, build clean |
| 2026-06-06 | 12 | P6 Frontend 12c-12f review (12 rounds) | COMPLETED — 2 consecutive clean (R11+R12). 210 tests, tsc+build clean |
| 2026-06-06 | 12 | P6 Frontend 12g commit | COMPLETED — ee5f62d |
| 2026-06-06 | 12 | P7 Integration 12a/12b (Tasks 7.1-7.4) | COMPLETED — scanner "Backtest These Settings" buttons (ScanDetail + ScheduledScans) + scanSeed util (6 tests); golden-set (6 tests); performance <1.5s (2 tests); nav (done P6); carry-forward integration (3 DB-gated). 216 FE + 208 BE backtest tests, tsc+build clean |
| 2026-06-06 | 12 | P7 Integration 12c-12f review | IN_PROGRESS — R1-R3 fixed entry-fee/price-drift/funding/equity-curve-points. R4 (3 agents): equity-curve fix VERIFIED + adversarial found follow-on HIGH — curve started at first trade's CLOSE not the starting-capital anchor (single-trade run → degenerate drawdown); + MED non-chronological force-close tail. FIXED: seed (start, starting_capital) anchor before scan loop; stable-sort equity curve by ts before metrics; removed dead wallet_delta. Single losing trade now shows max_dd 5.11%. 214 BE backtest tests. NEXT: R5 verify.

---

| 2026-06-06 | 13 | Cross-Phase Validation R1 (5 agents) | Found 3 cross-phase bugs: (1) HIGH engine wrote drawdown_pct=0.0 placeholder → frontend drawdown chart flat-zero (fixed: backfill real drawdown-from-peak); (2) MED-HIGH mfe_pct/pnl_pct NUMERIC(8,4) overflow at high leverage → persist crash (fixed: migration 40 widen to NUMERIC(12,4)); (3) MED cancel→complete race (fixed: eager-cancel NOT EXISTS results guard). Architecture coherent, all suites green (215 BE/199 FE). |

| 2026-06-06 | 14 | Final Review R1 (5 agents) | Security: no SQLi/CSV-injection/secrets (proxy-aware rate-limit + warmup throttle noted as deployment config). Backend: production-ready single-worker (multi-worker scaling noted). Product: all FR/AC met. Maintainability: HIGH max_same_sector no-op + dead _compute_total_unrealized. Test: 4 close rules untested. FIXED: 3 close-rule golden tests (trailing/equity_drop/liquidation), max_same_sector_not_enforced warning + UI label, removed dead code + stale comment. 221 BE / 200 FE backtest tests. |

| 2026-06-06 | 14 | Final Review R2 (5 agents) | Two independent findings: HIGH — result warnings (e.g. max_same_sector_not_enforced) rendered ONLY on the no-results empty state, so successful runs with trades silently swallowed them. MED — trades-fetch error masqueraded as "No trades to display". Adversarial pass found NO production bugs (engine/service robust). FIXED: WARNING_LABELS map + warningLabel() + role=status result-warnings banner for any completed-with-metrics run; tradesQuery.isError branch with "Failed to load trades" + Retry. +2 FE tests. 202 FE backtest tests, tsc 0. |

| 2026-06-06 | 14 | Final Review R3 (2 agents) | Frontend-reviewer: "FRONTEND FIXES VERIFIED" (both R2 fixes correct, 202 tests, tsc 0) + 2 non-blocking polish items: unknown non-metrics_ codes rendered raw snake_case; Analysis tab still masked the same trades-fetch error (sibling bug class). Product-reviewer: "FEATURE COMPLETE — ready to merge" (221 BE/3 skip, 219 FE, tsc 0, build 0, all spec compliance met, no blockers). FIXED: warningLabel universal de-underscore; shared TradesFetchError on BOTH Trades + Analysis tabs; +1 regression test. 203 FE backtest tests, tsc 0. NEXT: R4 verify toward 2 clean. |

| 2026-06-06 | 14 | Final Review R4 (5 agents) | ALL FIVE CLEAN: Frontend "FRONTEND CLEAN" (203 tests; 2 a11y/UX nits noted non-blocking); Engine "ENGINE CORRECTNESS CLEAN" (per-position reconciliation traced exact for normal+liquidation; close-rule ordering/funding/multi-symbol all sound; 23 golden+engine pass); Service "SERVICE LIFECYCLE CLEAN" (no leak/double-count/orphan; _json_safe sound; one defense-in-depth nit: route _num through _finite); Adversarial "NO PRODUCTION BUGS FOUND" (funding boundary, degenerate metrics math, LTTB/persistence None-handling, zod↔Pydantic contract all defended); QA "TESTS SOLID" (golden assertions exact & non-tautological; new FE tests genuine regression guards) + 1 caveat: equity_rise/breakeven_timeout lacked golden reconciliation guard. RESOLVED caveat: confirmed both already tested behaviorally (test_engine_close_rules/advanced_rules/trading_rules); ADDED 2 golden scenarios (close_on_profit + breakeven-via-TP) for uniformity. 223 BE/3 skip, 203 FE. R4 not clean (added 2 tests). NEXT: R5 verify toward 2 clean. |

| 2026-06-06 | 14 | Final Review R5 (5 agents) | 4/5 CLEAN: Golden "GOLDEN TESTS VERIFIED" (R4's 2 new tests faithful + reconciling, 17 pass); Build "ALL GREEN" (BE 223×2 deterministic, FE 220, tsc 0, build 0); Spec "SPEC FULLY TRACED" (all 9 core reqs satisfied w/ file:function evidence); Maintainability "CLEAN — no merge blockers" (3 dead-code nitpicks + defaults-comment, all follow-up tier). **Adversarial found a SEVERE REAL BUG**: max_trades gated on LIFETIME signals_entered (engine:467, counter at :598 never reset between scans) → capped the ENTIRE multi-scan backtest at max_trades total, when production builds a fresh executor per scan (scanner_service:416/576, trades_executed=0) making it PER-SCAN. On a multi-scan run this under-counts trades/PnL by orders of magnitude → violates <1% deviation. VERIFIED independently against production + spec §FR-004. FIXED: added per-scan scan_entered counter (reset each scan at :164), gate max_trades on it (:467), kept signals_entered lifetime for target_goal(:475)+stats. Reviewer's suggested fix (len(open_positions)) was WRONG (concurrency≠per-cycle); used production-faithful per-scan reset instead. ADDED regression test test_max_trades_is_per_scan_not_lifetime (2 scans→4 trades); PROVED it fails under bug (got 2) + passes under fix (got 4) via temporary revert. Full BE suite 36 fail are ALL pre-existing/env (persistence isolation-flaky, close_positions 9 mock fails IDENTICAL at stashed baseline, ws debounce flaky) — my diff touches zero failing-suite files. 223 BE backtest + 70 filter/cycle + 203 FE green. R5 NOT clean (real bug fixed). NEXT: R6 verify toward 2 clean. |

| 2026-06-06 | 14 | Final Review R6 (5 agents) | 3/5 CLEAN: max_trades-fix "MAX_TRADES FIX VERIFIED" (reset placement/split/fill-pass/output-leak all correct, 95 pass); Build "BACKTEST SURFACE ALL GREEN" (BE 223×2 deterministic + 160 engine + regression; FE 203, tsc 0, build 0); Product readiness flagged the SAME blocker as adversarial (see below). **Adversarial (directed at same per-scan/lifetime bug CLASS) found a SIBLING BUG**: cycle_start_equity set only when ==0 and zeroed ONLY by equity rules (engine:946/956) → a cycle closing via TP/SL/trailing/max_duration/liquidation leaves a STALE prior-cycle baseline; next scan's equity_drop/close_on_profit measure against the wrong reference. Bidirectional, affects which trades close + reported PnL/metrics → violates <1% deviation. Product-reviewer INDEPENDENTLY flagged identical bug + "NOT READY — one blocker". VERIFIED against production (auto_trade_service:211 fetches CURRENT wallet into base_capital per scan = the EQUITY_DROP reference_value:267) + instrumented repro (scan-2 flat position: equity 8913 vs stale 9956 = -10.5% → wrongly fires equity_drop). FIXED: re-anchor `if not state.open_positions: state.cycle_start_equity = 0.0` at scan start (engine:175), letting next _open_position re-seed to current wallet; preserves anchor when positions carry over. ADDED regression test_equity_drop_reference_reanchors_each_cycle (capital_pct=80/SL=12 → scan-1 loses 10.5%, scan-2 flat must NOT equity_drop); PROVED fails under bug ('equity_drop'!='equity_drop') + passes under fix. Initially wrote a 2nd winning-cycle test but INSTRUMENTED it + found it produced identical results fix-on/off (non-discriminating) → REMOVED it rather than ship a vacuous guard. **Production-parity audit (1 agent) flagged 4 divergences (signal-age ref-frame, price-drift symmetry, sizing wallet-vs-available, immediate-fill gap) — MUST verify independently next.** 223 BE backtest + 161 engine + 203 FE green. R6 NOT clean (sibling bug fixed). NEXT: verify parity claims, then R7. |

| 2026-06-06 | 14 | R6 Parity-Verification (3 targeted agents) | Independently verified all 4 production-parity claims against BOTH production + backtest code. RESULTS: (1) **signal-age = ACCEPTABLE** — verifier PROVED production fires auto-trade INLINE seconds after scan completion (scanner_service:790, not "hours later" as claimed); per-ticker completed_at isn't even stored in scan_results, age≈0 is faithful to near-immediate execution, no faithful fix constructible from stored schema → DOCUMENT as known replay limitation, no code change. (2) **price-drift = REAL, FIXED** — production is direction-aware/signed (auto_trade_service:1034-1039: buy rejects only on +drift, sell only on −drift; a favorable adverse-move entry is ADMITTED) but backtest used symmetric abs() → wrongly rejected better entries. Fixed engine:510 to signed/directional mirroring production; ADDED test_golden_price_drift_is_directional (buy-dropped admitted, buy-ran-up + sell-dropped rejected); PROVED fails under abs() (admitted buy got 0 trades). (3) **sizing-basis = REAL, FIXED** — production sizes off totalAvailableBalance (wallet − locked margin, accounts_service:251) but backtest sized off FULL wallet; with default capital_pct=5%/lev20 + skip_if_positions_open=False (default), each carried position oversized next scan by ~capital_pct% (5×+ tolerance). Fixed engine:206 sizing_capital = wallet − sum(open locked_margin); ADDED test_sizing_uses_available_balance (ETH qty 5.326 w/ fix vs 6.659 bug); PROVED discriminates. (4) **immediate-fill = REAL, FIXED** — production fill_immediate_remaining (auto_trade_service:472) relaxes filters to top immediate-mode cycles to max_trades, backtest _process_immediate_signals was strict-only. Fixed engine:380 added relaxed backfill pass mirroring batch pass; ADDED 2 tests (backfill-to-cap, respects-cap); PROVED fail under bug (entered 1 vs 2/3). 224 BE backtest + 165 engine + 203 FE green, deterministic. 3 real parity bugs fixed + 1 dispositioned. NEXT: R7 verify toward 2 clean. |

| 2026-06-06 | 14 | Final Review R7 (5 agents) | 4/5 CLEAN: backend-reviewer "R6 FIXES VERIFIED" (all 4 R6 fixes correct/complete/production-faithful, 49 pass); Build "ALL GREEN" (BE 224×2 deterministic + 165 engine + 7 R6 regression; FE 203, tsc 0, build 0); QA "TESTS SOLID" (all 8 new regression tests genuine discriminators w/ hand-verifiable constants; no existing test weakened; 1 minor coverage-asymmetry noted: sell-UP admit untested); Product "READY TO MERGE" (admission+sizing parity now shared-code-backed + regression-locked; accounting exact by golden invariant; calibrated residual-risk: candle-granularity close-timing + sector no-op, both bounded+warned). **Adversarial found ANOTHER parity bug**: backtest anchored qty AND TP/SL to the SLIPPED entry price, but production sizes qty off raw mark_price + anchors TP/SL off raw mark_price (accounts_service:254/290-299, entry only FILLS at slipped avgPrice). Bias ~lev×slippage/trade favorable, systematic → ~0.02%/trade equity at default (capital5%/lev20/slip2bps) → exceeds 1% over 50+ trades. Reviewer ALSO claimed liq-to-mark + add-exit-slippage. VERIFIED via focused agent: anchor bug REAL (fix = 2 lines: qty + TP/SL use entry_base_price); liq KEEP slipped (Bybit liquidates off avgPrice — reviewer's over-reach, moving it would ADD divergence); exit-slippage DON'T add (exact-trigger fill is TradingView-standard + would double the user's slippage knob — over-reach); invariant SAFE (changes inputs not accounting identity). FIXED engine:600-627 (qty off entry_base_price, TP/SL off entry_base_price, liq stays entry_price/slipped, entry_price slipped fill kept for PnL/fee/margin). ADDED test_golden_slippage_anchors_tp_sl_and_qty_to_unslipped_mark (mark50000/slip10bps: qty 0.4 off mark not 0.3996, TP exit 52500 off mark not 52552.5, pnl 980, reconciles); PROVED fails under bug (qty 0.399). Golden tests use slippage_bps=0 so unaffected. 225 BE backtest + 166 engine + 203 FE green. R7 NOT clean (parity bug fixed). NEXT: R8 toward 2 clean. COMMITTED R2-R7 as e8d1ce3 (701 ins, 10 files). |

| 2026-06-06 | 14 | Final Review R8 (3 agents) | 2/3 CLEAN: close-rule-timing "CLOSE-RULE ORDER/TIMING CORRECT" (per-candle order matches spec FR-004 funding→liq→TP/SL→equity→trailing→time; no double-close; liq-vs-SL precedence correct; trailing state machine + max_duration/breakeven all correct) + 1 non-blocking defensive rec (add `pos in open_positions` re-check to trailing+time close loops); Build "ALL GREEN" (BE 225×2 deterministic + 166 engine + 3 perf; FE 220, tsc 0, build 0, git clean @e8d1ce3). **Adversarial found exit-slippage missing**: apply_slippage called ONLY on entry (engine:599), never exit — all closes fill at exact trigger/close while production closes via Bybit market reduce-only orders (bybit_client:353) that slip adversely. ~0.02%/trade favorable, systematic → ~1% over 50 closes. This DIRECTLY CONTRADICTED R7's focused verifier (which said DON'T add exit slippage — TradingView-standard exact-fill + knob-doubling). Genuine design conflict → resolved via spec check + **AskUserQuestion**: spec lines 99-103 explicitly said exact-exit-fills (entry-only slippage), BUT user chose **"Apply exit slippage too (production-faithful)"** — slippage_bps is round-trip, overriding the spec (justified: prime directive is <1% deviation from REAL trading, production demonstrably slips on exit). FIXED: added SimulationState.slippage_bps (set in run()), apply_slippage adversely on exit in _close_position non-liq branch (close_side = inverse of position side); liquidation exempt (full-margin loss, not a fill); exit_price reassigned to slipped fill for the trade record. UPDATED spec lines 99-103 (round-trip) + 170-191 (qty/TP/SL off mark, entry/liq slipped) + 110 (directional drift). UPDATED R7 anchor test (exit now 52447.5/pnl 959 w/ exit slip). ADDED test_golden_exit_slippage_applied_adversely_on_close (SL: trigger 48000→fill 47904, pnl -878.4 vs -840 no-slip); PROVED both fail under revert. APPLIED the defensive guard (pos in open_positions) to trailing+time close loops. 226 BE backtest + 167 engine + 3 perf + 203 FE green. R8 NOT clean (bug fixed). NEXT: R9 toward 2 clean. |

| 2026-06-06 | 14 | Final Review R9 (3 agents) | 2/3 CLEAN: exit-slippage-fix VERIFIED (direction correct, reconciles, all non-liq closes covered, liq exempt; 48 pass); Build ALL GREEN (BE 226x2 deterministic + 167 engine + 3 perf; FE 220, tsc 0, build 0, git clean @90f2a92). ADVERSARIAL found a SUBTLE INTERACTION BUG between the two R6 fixes: cycle_start_equity re-anchored ONLY on empty book + to FULL wallet, but the R6 sizing fix re-anchors sizing_capital EVERY scan to wallet-locked. Production rebuilds the executor per scan + recreates the EQUITY_DROP rule EVERY scan with reference_value=totalAvailableBalance (wallet-locked) even mid-cycle with carried positions (skip_if_positions_open=False). So with carried positions the backtest reference was too HIGH -> equity_drop fired too eagerly. CONTRADICTS my own R6 comment (preserve-anchor-on-carryover intuition was WRONG vs production). VERIFIED via focused agent (auto_trade_service:211 base_capital=totalAvailableBalance re-fetched every scan; :234 rules_created_for per-executor->always recreates; close_rule_evaluator:158 equity=totalEquity, :375 drop=(ref-equity)/ref). Worked ex: W=10000 locked=2000 upnl=-500 -> prod -18.75pct (no trip) vs buggy +5pct (false trip). FIX: re-anchor cycle_start_equity=sizing_capital (wallet-locked) EVERY non-skipped scan (after skip-continue so skip-path preserves anchor=prod only no-recreate case); reduces to full wallet on empty book. FIXED engine:220 + cleaned stale R6 comment + _open_position fallback seed wallet_balance->sizing_capital. ADDED test_equity_drop_reference_excludes_carried_locked_margin (BTC carried locks margin, ETH modest loss: FIX->backtest_end, BUG->equity_drop); PROVED fails under revert. UPDATED spec equity-ref section. 226 BE backtest + 168 engine + 203 FE green. R9 NOT clean (interaction bug fixed). NEXT: R10 toward 2 clean. |

| 2026-06-06 | 14 | Final Review R10 (3 agents) | 2/3 CLEAN: R9-fix-verify R9 FIX VERIFIED (placement after skip-continue correct, no SMART-reanchor clobber, no div-by-zero window, single-scan reduction correct, reconciles; 62 pass); Build ALL GREEN (BE 226x2 deterministic + 168 engine + 3 perf; FE 220, tsc 0, build 0, git clean @bc73a5a). ADVERSARIAL found R9 was INCOMPLETE (deeper layer of same vein): the available basis omitted the +unrealised_pnl term. Bybit totalAvailableBalance = totalWalletBalance + totalPerpUPL - totalInitialMargin; R9 subtracted IM but omitted +uPNL. So with carried positions having non-zero uPnL the basis (sizing + equity-ref) was off by carried uPnL. Cited account_context.py:132 + accounts_service.py:733 (margin_used=wallet-available only positive when uPnL>0 -> available=W+uPnL-IM). VERIFIED via focused agent w/ full algebra: drop_prod=-IM/(W+U-IM), drop_bt=-(IM+U)/(W-IM), delta=U(W+U)/[(W+U-IM)(W-IM)] (~5pp, entirely from U); structurally hard to FLIP a 10pct trigger via uPnL alone (both refs ~25pct below equity) BUT violates <1pct parity + MIS-SIZES every carried cycle (~3pct at lev10) which compounds. Proposed fix (ref=W+U-IM) makes drop_bt==drop_prod EXACTLY; residual ~0.4pct mark-vs-entry IM (optional). FIX: compute available_balance = wallet + carried_upnl - locked_margin ONCE (marked at candle<=current_time, no look-ahead) feeding BOTH sizing_capital AND cycle_start_equity. FIXED engine:209-237. ADDED test_sizing_basis_includes_carried_unrealized_pnl (BTC carried winner +300 uPnL -> ETH sizes off 6300 -> qty 4.2 not 4.0); PROVED fails without +uPnL term (4.0!=4.2). UPDATED spec equity-ref. 226 BE backtest + 172 engine/perf + 203 FE green, deterministic. R10 NOT clean (incomplete-fix completed). NOTE: this vein has now had 4 consecutive rounds of findings (R7-R10) each deeper; R10 residual is a known ~0.4pct mark-vs-entry IM bound. NEXT: R11 toward 2 clean. |

| 2026-06-06 | 14 | Final Review R11 (3 agents) | 2/3 CLEAN: R10 FIX VERIFIED (carried_upnl look-ahead-safe; sizing+ref from one available_balance; empty-book reduction; reconciles; perf bounded by candle-break, heavy 0.66s; 48 pass) + residual: IM mark-vs-entry ~0.4pct CANNOT compound (self-corrects each scan) -> LEAVE documented; Build ALL GREEN (BE 226x2 det + 169 engine + 3 perf 0.66s; FE 220, tsc 0, build 0, git @13c12d3). ADVERSARIAL: equity-reference vein CONVERGED + pivoted OUT; units/contract + cancellation DEFENSE; FOUND 1 LOW bug OUTSIDE vein: _downsample_equity force-include keyed min-EQUITY not min-DRAWDOWN_PCT (backtest_service:478) -> peak-rises-after-early-low curve, LTTB drops true max-dd point, drawdown overlay shallower than max_dd_pct tile (falsifies stated invariant). FIXED :478 keys drawdown_pct + docstring. REWROTE test_downsample_preserves_max_drawdown_point (n=20000, deep -77pct dd at collinear point LTTB drops): PROVED fails under old key (-0.001 vs -77). 226 BE + 169 engine + 203 FE green, deterministic, perf OK. R11 NOT clean (1 low bug fixed). NEXT: R12 toward 2 clean. |

| 2026-06-06 | 14 | Final Review R12 (3 agents) | 2/3 CLEAN: correctness-cold-read CORRECTNESS CLEAN (R11 fix verified: min(drawdown_pct) and max(positive dd) resolve to SAME point; cold whole-engine read of finalization/metrics/funding/MFE-MAE/reconciliation found nothing; 226 pass); Build ALL GREEN (BE 226x2 det + 194 engine/service + 3 perf 0.73s; FE 640, tsc 0, build 0, git @f7bf10a). ADVERSARIAL (directed at service/API/frontend seams) found 2 issues + dispositioned rest (no-auth=deliberately single-tenant; cancel race holds; toast/pagination sound): (1) Low-Med RATE-LIMIT TOCTOU — _check_rate_limit checked window before INSERT await but _record_create consumed token only AFTER -> concurrent creates from one client share a token, over-admit by up to MAX_CONCURRENT-1. FIXED: _reserve_create_token (atomic check+consume synchronously before first await) + _refund_create_token on busy/fail; removed dead _check_rate_limit/_record_create; reordered candle-check before reserve. ADDED 3 tests (atomic-no-overshoot, busy-refunds, insert-fail-refunds) + updated the post-launch test. (2) Med CONFIG-DEFAULT DIVERGENCE — zod and Pydantic disagreed on ~10 defaults (leverage 1vs20, sim_interval 1h vs 5m, TP 10vs150, etc.); backend unauthenticated so a minimal POST ran an aggressive sim unlike the form. **AskUserQuestion -> user: ~100% real-world results**. Backend defaults ALREADY = production AutoTradeConfig (the real-world source), so ALIGNED FRONTEND zod -> backend/production defaults; ALSO fixed backend backtest execution_mode batch->immediate to match production AutoTradeConfig. ADDED backend guard test_defaults_match_production_autotradeconfig (asserts all 27 shared fields equal production) + updated FE configSchema minimal-defaults test. 230 BE backtest + 203 FE green, tsc 0, build 0. R12 NOT clean (2 issues fixed). NEXT: R13 toward 2 clean. |

| 2026-06-06 | 14 | Final Review R13 (3 agents) | 2/3 CLEAN: R12-fix-verify R12 FIXES VERIFIED (rate-limit reserve atomic/refund-on-all-pre-launch-rejections/no-double-refund/evicted-token-safe; defaults aligned; guard test correct) + found engine execution_mode FALLBACK still batch (latent, I introduced in R12 default-flip); Build ALL GREEN (BE 230x2 det + 255 engine/service/schema + 3 perf 0.83s; FE 640, tsc 0, build 0, det run1=run2). ADVERSARIAL (whole-feature integration) found HIGH MERGE-BLOCKER: cross-layer contract bug — frontend TRADES_PAGE_LIMIT=1000 (BacktestResultsPage:93) but router caps limit le=500 (backtest:116) + service clamps min(limit,500):1050 -> FastAPI 422s BEFORE handler -> EVERY completed runs Trades AND Analysis tabs error out (only Overview/Equity work since they read run.results). 12 rounds missed it: router tests use limit=50, FE tests MOCK getTrades -> the contract seam never exercised end-to-end. + LOW: Analysis tab no truncation disclosure (follow-up). Defenses confirmed: max_dd_pct sign trace all 4 layers reconciles; concurrency cancel/timeout/rate-refund sound. FIXED: (1) TRADES_PAGE_LIMIT 1000->500 (BacktestResultsPage:93) — keeps Showing-first-N banner; ADDED FE test asserting request limit<=500 (captures real MSW request URL, the seam mocks miss); PROVED fails at 1000 (expected 1000 <= 500). (2) engine execution_mode fallback batch->immediate (backtest_engine:161) to match contract. 230 BE backtest + 204 FE green, tsc 0, build 0. R13 NOT clean (1 HIGH + 1 LOW fixed). NEXT: R14 toward 2 clean. |

| 2026-06-06 | 14 | Final Review R14 (3 agents) | **FIRST CLEAN ROUND**. Integration-reviewer CONTRACT CONSISTENT (systematically audited EVERY frontend<->backend seam: create/list/compare/get/trades/cancel/delete/cache; R13 limit fix verified 500<=500; create body matches schema, all query values within ge/le/enum bounds, all response fields exist, statuses handled; NO second instance of the limit-mismatch class; 2 LOW non-contract notes: >500-trade client-side truncation by design, dead sort_by/side params). Adversarial NO MATERIAL BUGS FOUND merge-ready ~85pct confidence (chased 5 leads: signal_score int/float typing DEFENDED via INTEGER source col, compare 2-4 limit DEFENDED service-enforced, polling terminal/404 DEFENDED default-deny, compare label/color ordering DEFENDED request-order, cache_status DEFENDED; only cosmetic rough edges: scanSeed >365d span surfaces clean 422, formatPct negative-zero). Build ALL GREEN (BE 230x2 det + 111 router/service/schema + 3 perf 0.79s; FE 641, tsc 0, build 0, git @d2d479a). NO fixes needed. Cleaned 1 stale doc comment (1000->TRADES_PAGE_LIMIT, comment-only, no behavior). NEXT: R15 must also be clean for 2-consecutive-clean exit. |

## Key Decisions

- No AI Manager in backtesting (deferred)
- Uses real scan results from DB as signal source
- User provides fresh capital/TP/SL/leverage (no account configs)
- Must be <1% deviation from real trading
- Super fast execution (seconds, not minutes)
- TradingView-quality metrics and charts

---

## Files Created

| File | Step | Purpose |
|------|------|---------|
| plans/backtesting-system/progress-tracker.md | 1 | This file |
| plans/backtesting-system/discovery-summary.md | 1 | Codebase discovery findings |
| specs/backtesting-system-requirements.md | 2 | 305 requirements (all rounds) |
| specs/backtesting-system-architecture.md | 3 | Architecture + review fixes |
| specs/backtesting-system-spec.md | 4-5 | Full specification (reviewed + clean) |
| plans/backtesting-system/implementation-plan.md | 6-7 | Implementation plan (reviewed + clean) |

---

## Phase 5 Carry-Forward (raised during P4 reviews — MUST address when wiring service)

### Phase 6 Task 6.3 Carry-Forward (deferred UX polish — non-blocking)
- Quick/Advanced mode toggle, preset save/load (localStorage import/export JSON), and dirty-form
  navigation guard (useBlocker / beforeunload) from plan Task 6.3 are NOT implemented. All config
  fields ARE reachable via the collapsible Section UI (functional parity), so this is UX polish, not
  a correctness/feature gap. Product reviewer (R5) judged it defer-able. Track as a follow-up; the
  form already accepts a `seed` prop so presets can be layered on later without refactor.
- Equity chart crosshair/zoom (recharts Brush) from Task 6.5 also deferred (nice-to-have); Buy & Hold
  line IS now implemented.

### Phase 7 Integration-Test Carry-Forward (raised during P5 12f testing review)
- The Phase 5 unit suite (~96 tests) mocks `db.pool` and `_wire_transaction` (codebase norm). It does
  NOT exercise real asyncpg SQL/rollback/concurrency. Phase 7 integration should add ~4 Postgres-backed
  tests: (1) persist round-trip read-back, (2) transaction rollback on trade-insert failure leaves no
  orphan results, (3) genuinely concurrent runs respecting the 3-slot cap, (4) the cross-thread
  _on_progress → call_soon_threadsafe DB-write hop. Not a Phase 5 blocker (unit gate uses mocks).

1. **excess_return + Buy & Hold wiring** — `compute_buy_hold_return(btc_klines, capital)` exists and is
   correct, but `compute_all_metrics(trades, equity, config)` takes no `btc_klines`, so it is NOT wired.
   Spec FR-006 "Comparison" row requires Buy & Hold return AND `excess_return` (= net_profit_pct −
   buy_hold.return_pct). Phase 5 service must: fetch BTCUSDT klines for the window, call
   `compute_buy_hold_return`, and add both `buy_hold_return_pct` and `excess_return` to the results
   payload. excess_return has ZERO implementation today — do not silently drop it.

2. **per_trade persistence — avoid JSONB duplication** — `compute_all_metrics` output now includes a
   `per_trade` list (one entry per closed trade) for the cumulative-PnL chart. Its fields are a near-exact
   subset of the `backtest_trades` TABLE columns. Do NOT persist the 50k-entry `per_trade` array into the
   `backtest_results.metrics` JSONB cell (multi-MB, TOASTed, shipped whole on every fetch). Instead:
   persist per-trade rows to `backtest_trades`, and serve the cumulative-PnL series from that table
   (compute `cumulative_pnl` on read, ordered by exit_time). Keep `metrics` JSONB to scalar aggregates.
   Consider capping/downsampling `per_trade` in the API response like the equity curve (LTTB).
